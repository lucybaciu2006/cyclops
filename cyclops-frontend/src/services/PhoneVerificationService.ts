import {axiosInstance} from '@/lib/axios.config.ts';

export class PhoneVerificationService {

    // public static async startPhoneVerification(phoneNumber: string): Promise<ExternalPropertyDetails> {
    //     const response = await axiosInstance.post<ExternalPropertyDetails>(`/phone-verification/start`, {phoneNumber});
    //     return response.data;
    // }


}
